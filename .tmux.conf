set-option -g default-shell "/bin/zsh"
setw -g mouse on

# set prefix to ctrl+a
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# reload tmux config with prefix + R
bind-key R source-file ~/.tmux.conf \; display-message "tmux.conf reloaded."

# base index windows to 1
set -g base-index 1
set -g pane-base-index 1

# automatically set window titles
set-window-option -g automatic-rename on
set-option -g set-titles on

# vi keybindings
setw -g mode-keys vi
# bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# history
set -g history-limit 10000

# message display
set -g display-time 500
# set -g message-style 'fg=black bg=colour42 '

# status bar config
# set -g status "on"
# set -g status-interval 5
# set-option -g status-style fg=colour42,bg=black
# set-window-option -g window-status-current-style fg=black,bg=colour42

# set -g status-justify centre
# set -g status-right '#{prefix_highlight}'
# set -g status-right '#{prefix_highlight} #(uptime -p)'
# set -g @prefix_highlight_fg 'black' # default is 'colour233'
# set -g @prefix_highlight_bg 'gray'  # default is 'colour04'
# set -g @prefix_highlight_empty_prompt '    '

# Default statusbar color
# set-option -g status-style bg='black',fg='gray' # bg=bg1, fg=fg1
# set-option -g status-style bg=colour237,fg=colour223 # bg=bg1, fg=fg1

# # Default window title colors
# set-window-option -g window-status-style bg=colour214,fg=colour237 # bg=yellow, fg=bg1

# # Default window with an activity alert
# set-window-option -g window-status-activity-style bg=colour237,fg=colour248 # bg=bg1, fg=fg3

# # Active window title colors
# set-window-option -g window-status-current-style bg=red,fg=colour237 # fg=bg1


# set-option -g default-command "reattach-to-user-namespace -l $SHELL"

# panes
# set -g pane-border-style fg=gray
# set -g pane-active-border-style "bg=default fg=colour42"
bind-key space choose-tree
bind-key v split-window -h
bind-key s split-window -v
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

################################## SET COLORS ##################################
color_bg="#1e1e1e"
color_fg="#ffffff"
color_green="#98c379"
color_yellow="#fac863"
color_red="#e06c75"
color_blue="#61afef"
color_cyan="#56b6c2"
color_purple="#c594c5"
color_gray="#696C77"
color_selection="#3e4452"

text_normal=$color_gray
text_highlight=$color_green
text_emphasise=$color_purple
text_red=$color_red
text_green=$color_green
text_yellow=$color_yellow

################################## SET OPTIONS #################################

# set -g status-bg "#000000"
set -g status-justify "left"
set -g status-left-length "100"
set -g status "on"
set -g status-right-length "160"
set -g status-right-style none
set -g message-style bg=$color_green,fg=$color_bg
set -g status-style none
set -g pane-border-style fg=$color_gray
set -g pane-active-border-style fg=$color_gray
set -g status-left-style none
setw -g window-status-style bg=default,fg=$color_bg,none
setw -g window-status-current-style bg=default,fg=$color_bg
setw -g window-status-activity-style fg=$color_green,none
setw -g window-status-separator ""
set-window-option -g mode-style bg=$color_selection,fg=$color_fg
# set -g @prefix_highlight_bg $color_blue # blue
# set -g @prefix_highlight_fg $color_bg # white / bg

# tmux-online-status
# set -g @route_to_ping "vultr.net"   # Use a UK based site to ping
# set -g @online_icon ""
# set -g @offline_icon "#[fg=$color_red]"

################################## STATUSLINE ##################################

set -g status-left " #[fg=$text_emphasise,bold] [#S]"

# set -g status-right "#(battery -a -p -t -g '$text_green' -m '$text_yellow' -w '$text_red')  #[fg=$color_blue] #(uptime -p) - #[fg=$color_blue]%R "
# set -g status-right "#(battery -a -p -t -g '$text_green' -m '$text_yellow' -w '$text_red')  #[fg=$color_blue]"
set -g status-right ""


setw -g window-status-format "#[fg=$text_normal,nobold,nounderscore,noitalics] #[fg=$text_normal] #I #W #[fg=$text_normal,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=$text_normal,nobold,nounderscore,noitalics] #[fg=$text_highlight] #I #W #[fg=$text_normal,nobold,nounderscore,noitalics]"


set-option -sg escape-time 10
set-option -g default-terminal "xterm-256color"
# set-option -sa terminal-overrides ',xterm-kitty:RGB'
set-option -g focus-events on
